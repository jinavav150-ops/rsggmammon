// 관리자 전용 화면 — **서버가 관리자에게만 index.html에 끼워 넣는다.**
// ⚠️ 정적 화이트리스트(ALLOWED)에 넣지 말 것. 넣으면 /admin.js 로 아무나 받아간다.
// 일반 방문자가 받는 index.html에는 이 코드가 한 글자도 들어가지 않는다.
//
// 2026-09-05: '개명' 화면은 index.html로 옮겨 **모든 이용자에게 공개**했다.
// (여기 남겨두면 관리자에게만 탭이 두 개로 보인다.)
// 관리자 전용 기능이 다시 생기면 이 파일에 넣는다 — 주입 구조(//__X__)는 그대로 살아 있다.
(function(){ /* 관리자 전용 기능 없음 */ })();
